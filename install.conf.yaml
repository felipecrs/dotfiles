- clean: []
- link: {}
- shell:
    - command: sudo apt install zsh
    - command: '[[ ! -d "$ANTIGEN_DIR" ]] && git clone https://github.com/zsh-users/antigen.git "$HOME/.antigen"'
    - command: chsh -s $(which zsh)
