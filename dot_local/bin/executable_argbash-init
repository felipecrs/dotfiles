#!/bin/sh

if [ "$(command -v localpath)" ]; then
    volume="--volume=$(localpath "$PWD"):/wd"
else
    volume="--volume=$PWD:/wd"
fi

exec docker run --rm "$volume" --user="$(id -u):$(id -g)" -e PROGRAM=argbash-init matejak/argbash "$@"
