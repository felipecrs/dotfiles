#!/bin/bash

function echo_task() {
  printf "\033[0;34m--> %s\033[0m\n" "$*" >&2
}

readonly args=({{ range (rest .chezmoi.args) }} {{ . | quote }}{{ end }})

echo_task "Running: rootmoi ${args[*]}"
exec '{{ joinPath .chezmoi.homeDir ".local/bin/rootmoi" }}' "${args[@]}"
